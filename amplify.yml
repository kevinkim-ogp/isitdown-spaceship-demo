version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - export NODE_ENV=production
        - env >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
